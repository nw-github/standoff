export default defineAppConfig({
  ui: {
    primary: "red",
    popover: {
      base: "[@media(pointer:coarse)]:!block",
    },
    tooltip: {
      base: "[@media(pointer:coarse)]:!block",
    },
  },
});
